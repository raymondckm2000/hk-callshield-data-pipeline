version: '3.9'
services:
  api:
    image: python:3.11
    working_dir: /code
    volumes:
      - .:/code
    command: bash -c "pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
